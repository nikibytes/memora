# Contributing — {{project_name}}

## Before you start
Read `docs/prd.md` and `docs/architecture.md`.

## Workflow
1. Pick or add an item in `docs/task-tracker.md`.
2. Branch per `docs/commit.md` naming convention.
3. Write code + tests, run `{{test_command}}`.
4. Open a PR using the template — link the task-tracker item.
5. Get review from the relevant `CODEOWNERS` path owner.

## Code style
{{style_notes}}

## Questions
{{contact_or_channel}}
