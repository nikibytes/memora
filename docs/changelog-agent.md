# Agent Changelog

2026-08-02 — Built PWA React UI (Landing page, Login page, and Main Screen showing Instagram collections) with tweakcn theme integration and logo — Build PWA React UI: Landing page, Login page, and Main Screen showing Instagram collections
2026-08-04 — Wired login flow end-to-end: disclaimer, Puppeteer auto-login, 2FA two-step handshake, SQLite dump, 3 API routes — Wire Instagram login flow (sessionHelper, db, orchestration routes, login-form)
2026-08-04 — Removed react-native deps, fixed build (Turbopack error), fixed fetch URL port 5678→8000 and request body shape — Fix build and API wiring
2026-08-04 — Setup isolated backend Python venv (uv-managed CPython 3.11, fastapi + uvicorn + instagrapi + debugpy) — Setup backend Python venv
